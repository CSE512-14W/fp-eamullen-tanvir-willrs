Energy Data Server
==================

A Python server for streaming home energy usage via a websocket
connection.

Installation
------------

```bash
pip install -r requirmeents.txt
python server.py
```
